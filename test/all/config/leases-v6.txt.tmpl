# Static DHCPv6 leases for the all-plugins test, one "MAC IP" pair per line.
#
# The file plugin runs with its default key:mac here, so the first field is
# the client's hardware address and the plugin recovers it from the DUID-LL
# the exerciser sends.
@MAC_FILE6@ @V6LAN@::11

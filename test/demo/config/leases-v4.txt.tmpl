# Static leases for the demo, one "MAC IP" per line. The addresses sit below
# docker's ip_range and the range pool, so nothing else can already hold them.
@MAC_A@ @NET@.11
@MAC_B@ @NET@.12
@MAC_C@ @NET@.13

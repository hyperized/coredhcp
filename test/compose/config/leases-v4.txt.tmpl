# Static leases for the compose test, one "MAC IP" per line. The addresses sit
# below docker's own ip_range, so nothing on the bridge can already hold them.
@MAC_A@ @NET@.11
@MAC_B@ @NET@.12
@MAC_C@ @NET@.13
